{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "pnpm install --frozen-lockfile && pnpm --filter @openota/server prisma:generate && pnpm turbo build --filter=@openota/server"
  },
  "deploy": {
    "startCommand": "pnpm --filter @openota/server start:prod"
  }
}
