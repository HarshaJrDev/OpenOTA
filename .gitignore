# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# Dependencies
node_modules
.pnp
.pnp.js

# Local env files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Testing
coverage

# Turbo
.turbo

# Vercel
.vercel

# Build Outputs
.next/
out/
build
dist


# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Misc
.DS_Store
*.pem

node_modules
dist
build
coverage
.env
.sqlite
*.sqlite
*.sqlite3
*.db
*.log
.turbo
apps/server/storage
# apps/e2e scratch workspace used by fixture generation
.e2e-work/
examples/OpenOTAExample

# pnpm/npm pack output — regenerate with `pnpm pack`, never commit
packages/*/*.tgz

# Sibling project used for real-device consumer testing; not part of this workspace
/PebloBuddy/
/ResumeIn/
*.fileloc

# Gradle's local build cache — machine-specific, regenerated on every build
.gradle/

# `openota build`/`openota release` output (bundleOutput in openota.config.json) — generated
# artifacts (JS bundle, zip, manifest), never source
apps/example/openota/
examples/OpenOTAExample
examples/OpenOTAExample
apps/server/dev.db-shm
apps/server/dev.db-wal

# Embedded PGlite local data dir
data/pgdata/
apps/server/data/
.env*

# NearDeals_Mobile is its own separate git repo (github.com/knkgroupservices/NearDeals_Mobile),
# just nested inside this folder on disk for local convenience — never part of this repo.
NearDeals_Mobile/
